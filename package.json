{
  "name": "budget-atlas",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "An interactive examination of how Americans live on what they earn — across cities, tax regimes, and household configurations.",
  "packageManager": "yarn@4.9.1",
  "scripts": {
    "start": "vite",
    "dev": "vite",
    "dev:prod": "AUDIT_PROXY_TARGET=https://thebudgetatlas.com vite",
    "dev:tunnel": "./scripts/tunnel.sh",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "verify": "yarn typecheck && yarn lint && yarn format:check && yarn test",
    "check-links": "./audit/links/check.sh",
    "audit:seed-issues": "node audit/links/seed-issues.mjs",
    "audit:staleness": "node audit/staleness/seed-issue.mjs",
    "audit:inventory": "node --experimental-strip-types scripts/source-inventory.mjs",
    "dev:worker": "wrangler dev --local --persist-to=.wrangler/state",
    "dev:worker:seed": "wrangler d1 execute budget-atlas-audit --local --file=worker/schema.sql --persist-to=.wrangler/state",
    "dev:local": "concurrently --names worker,site --prefix-colors blue,green --kill-others-on-fail \"yarn dev:worker\" \"AUDIT_PROXY_TARGET=http://localhost:8787 yarn dev\"",
    "dev:local:fresh": "yarn dev:worker:seed && yarn db:sync && yarn dev:local",
    "db:sync": "op run --env-file=.env.audit -- ./audit/links/sync-d1.sh",
    "db:apply-schema": "op run --env-file=.env.audit -- ./audit/links/apply-schema.sh"
  },
  "dependencies": {
    "@fontsource-variable/fraunces": "^5.3.0",
    "@fontsource/ibm-plex-mono": "^5.3.0",
    "@fontsource/ibm-plex-sans": "^5.3.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "react-is": "^19.2.8",
    "recharts": "^3.10.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260624.1",
    "@eslint/js": "^10.0.1",
    "@types/node": "^25.9.3",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "@vitejs/plugin-react": "^6.0.5",
    "concurrently": "^9.2.1",
    "eslint": "^10.8.1",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-react-refresh": "^0.5.4",
    "prettier": "^3.9.6",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.67.0",
    "vite": "^8.2.1",
    "vitest": "^4.1.10",
    "wrangler": "^4.123.0"
  }
}
